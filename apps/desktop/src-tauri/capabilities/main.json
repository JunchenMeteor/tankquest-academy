{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only Tauri core defaults are exposed to the TankQuest main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
